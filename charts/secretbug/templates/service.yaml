apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretbug.fullname" . }}
  labels:
    {{- include "secretbug.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretbug.selectorLabels" . | nindent 4 }}
